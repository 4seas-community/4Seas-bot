# Managed by the 4Seas Bot admin UI.
# Safe to hand-edit — but comments are dropped when the UI next rewrites this file.

- command: wifi
  description: Venue Wi-Fi details
  reply: |-
    📶 <b>Wi-Fi</b>
    Network: <code>network name</code>
    Password: <code>123456</code>

    Trouble connecting? Ask whoever's running the venue.
  enabled: true
  admin_only: false
  scope: all
  parse_mode: HTML
  disable_preview: true
